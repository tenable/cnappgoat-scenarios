name: cwpp-gcp-vulnerable-container-tomcat-on-artifact-registry
runtime: go
description: This scenario creates an artifact registry and uploads a vulnerable Tomcat image. Issues arise from vulnerable images in the registry. Remediation includes deleting the vulnerable
  images and uploading secure ones.
cnappgoat-params:
  module: cwpp
  platform: gcp
  id: cwpp-gcp-vulnerable-container-tomcat-on-artifact-registry
  friendlyName: Vulnerable Container Tomcat On Artifact Registry
  description: this scenario creates a new Artifcat Registry in GCP and copies a vulnerable Tomcat
    image onto it. The problem lies in the strategic placement of a compromised
    container, which could lead to potential security issues. To resolve these issues,
    it's important to maintain updated, non-vulnerable images.
  scenarioType: native