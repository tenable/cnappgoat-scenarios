name: ciem-aws-iam-overprivileged-group
runtime: go
description: Creates an IAM Group with overprivileged permissions. IAM Roles should be assigned the least amount of privileges necessary to perform their intended function.
cnappgoat-params:
  module: ciem
  platform: aws
  id: ciem-aws-iam-overprivileged-group
  friendlyName: IAM Group With Overprivileged Permissions
  description: Creates an IAM Group with overprivileged permissions. IAM Roles should be assigned the least amount of privileges necessary to perform their intended function. The scenario does this by creating a lambda and using the group access keys (shared via secret) with it.
  scenarioType: native

